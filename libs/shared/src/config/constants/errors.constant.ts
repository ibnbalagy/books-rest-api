
export class Errors {
  static NOT_FOUND_MSG: string = 'No entities found with given clause'
  static EMAIL_IN_USE: string = 'Email already in use'
  static INVALID_CREDENTIAL: string = 'Invalid credentials'
}